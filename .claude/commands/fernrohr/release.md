---
description: Release a repo - dispatch its Prepare Release workflow, auto-merge the release PR, and watch the rollout to healthy
argument-hint: "[repo-name] [ref]"
---

Invoke the `release-repo` skill for the repo named below. If no repo name was given, ask which
repo to release before proceeding - don't guess.

`$ARGUMENTS`
